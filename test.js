let reg = new RegExp(`ican't fall in love`);
// let reg = new RegExp('ican't fall in love');
// let reg = new RegExp("ican't fall in love");
console.log('\dsdwwwsa');

// 在字符串中的\d(仅表示一个d)和在字面量正则中的\d是不同的

console.log(/\d/)